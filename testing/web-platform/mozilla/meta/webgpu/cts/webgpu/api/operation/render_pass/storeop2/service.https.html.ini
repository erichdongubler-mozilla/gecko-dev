[service.https.html?worker=service&q=webgpu:api,operation,render_pass,storeop2:storeOp_controls_whether_1x1_drawn_quad_is_stored:*]
  implementation-status: backlog
  expected:
    if os == "win" and not debug: SKIP
    if os == "linux" and debug: SKIP
    if os == "mac" and not debug: SKIP
  [:storeOp="discard"]
    expected:
      if os == "win" and debug: FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac" and debug: FAIL

  [:storeOp="store"]
    expected:
      if os == "win" and debug: FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac" and debug: FAIL
