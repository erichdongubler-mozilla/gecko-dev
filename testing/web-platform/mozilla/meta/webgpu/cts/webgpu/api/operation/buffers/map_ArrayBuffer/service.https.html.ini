[service.https.html?worker=service&q=webgpu:api,operation,buffers,map_ArrayBuffer:postMessage:*]
  implementation-status: backlog
  expected:
    if os == "win": SKIP
    if os == "mac": SKIP
  [:transfer=false;mapMode="READ"]
    expected:
      if os == "linux": FAIL

  [:transfer=false;mapMode="WRITE"]
    expected:
      if os == "linux": FAIL

  [:transfer=true;mapMode="READ"]
    expected:
      if os == "linux": FAIL

  [:transfer=true;mapMode="WRITE"]
    expected:
      if os == "linux": FAIL
