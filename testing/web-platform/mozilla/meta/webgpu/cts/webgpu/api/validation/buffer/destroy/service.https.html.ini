[service.https.html?worker=service&q=webgpu:api,validation,buffer,destroy:all_usages:*]
  implementation-status: backlog
  expected:
    if os == "linux" and debug: SKIP
  [:]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL


[service.https.html?worker=service&q=webgpu:api,validation,buffer,destroy:error_buffer:*]
  implementation-status: backlog
  expected:
    if os == "linux" and debug: SKIP
  [:]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL


[service.https.html?worker=service&q=webgpu:api,validation,buffer,destroy:twice:*]
  implementation-status: backlog
  expected:
    if os == "linux" and debug: SKIP
  [:]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL


[service.https.html?worker=service&q=webgpu:api,validation,buffer,destroy:while_mapped:*]
  implementation-status: backlog
  expected:
    if os == "linux" and debug: SKIP
  [:]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL
